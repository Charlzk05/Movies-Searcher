<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>Charlzk Movies, TV Shows & Video Games Searcher - <%- name %></title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel="icon" type="x-image/icon" href="./movie.ico">
    <link rel='stylesheet' type='text/css' media='screen' href='global.css'>
    <link rel='stylesheet' type='text/css' media='screen' href='search.css'>
</head>
<body>
    <div class="main">
        <div class="background"></div>
        <div class="header">
            <h3>Results for <span><%- name %></span></h3>
            <div class="searchBar">
                <form method="get" action="/search">
                    <input name="name" type="text" placeholder="Search">
                    <input type="submit" value="Search">
                </form>
                <button onclick='window.location.href = "/";'>Home</button>
            </div>
        </div>
        <div class="listContainer">
            <div class="semiHeader">
                <h3>Movies, TV Shows & Video Games</h3>
            </div>
            <div class="list">
                <% for (var i = 0; i < results.length; i++) { %>
                    <% try { %>
                        <% if (results[i]["i"]["imageUrl"].slice(0, 5) == "https") { %>
                            <div class="item">
                                <img src='<%- results[i]["i"]["imageUrl"] %>'>
                                <div class="info">
                                    <p class="title"><%- results[i]["l"] %></p>
                                    <p class="type"><%- results[i]["q"] %></p>
                                    <p>Rank: <span class="rank"><%- results[i]["rank"] %></span></p>
                                    <p>Year: <span class="year"><%- results[i]["y"] %></span></p>
                                </div>
                            </div>
                        <% } %>
                    <% } catch (err) { %>
                        <% continue %>
                    <% } %>
                <% } %>
            </div>
            <div class="semiHeader">
                <h3>People</h3>
            </div>
            <div class="list">
                <% for (var i = 0; i < results.length; i++) { %>
                    <% try { %>
                        <% if (results[i]["s"] == "") {} else { %>
                            <div class="item">
                                <p><%- results[i]["s"] %></p>
                            </div>
                        <% } %>
                    <% } catch (err) { %>
                        <% continue %>
                    <% } %>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>